language: python
sudo: false
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
install:
  - pip install -U pip codacy-coverage
  - pip install -U .
  - pip install -U ".[test]"
script: nosetests --with-coverage --cover-package=xenon --cover-xml
after_script: python-codacy-coverage -r coverage.xml
